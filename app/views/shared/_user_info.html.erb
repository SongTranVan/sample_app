<%= link_to gravatar_for(current_user, size: Settings.current_user_avatar), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".profile_link"), current_user %></span>
<span><%= pluralize current_user.microposts.size, t(".mcrpost") %></span>
