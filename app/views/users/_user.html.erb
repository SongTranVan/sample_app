<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if force_delete? user %>
    | <%= link_to t("users.index.link"), user, method: :delete, data: { confirm: t("users.index.confirm") } %>
  <% end %>
</li>
